// import React from 'react';

// const UserInputSearch = (props) => {
//     return ( 
//         props.userInput === song.title || props.userInput === song.artist || props.userInput === song.album || props.userInput === song.release_date || props.userInput === song.genre
//      );
// }
 
// export default UserInputSearch;